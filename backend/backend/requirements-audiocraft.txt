# Alternative model stack: Meta's AudioCraft (the project spec's reference).
# Use this instead of requirements.txt when EMOJIMUSE_MODEL_BACKEND=audiocraft.
# AudioCraft pins older deps and is fussier on modern Colab — prefer the
# default transformers stack unless you specifically need parity with the spec.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.6,<3.0

audiocraft>=1.3.0
torch>=2.1
soundfile>=0.12
numpy>=1.24
regex>=2023.12
pyngrok>=7.0
nest-asyncio>=1.6
