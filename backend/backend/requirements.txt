# EmojiMuse backend — default (transformers) MusicGen stack.
# On Colab, torch is already present with CUDA; `pip install -r` will
# leave the existing build in place.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.6,<3.0

# --- model ---
torch>=2.1
transformers>=4.40
sentencepiece>=0.1.99        # T5 tokenizer used by MusicGen
accelerate>=0.27

# --- audio + misc ---
soundfile>=0.12
numpy>=1.24
regex>=2023.12               # grapheme-aware emoji splitting

# --- tunnel (Colab) ---
pycloudflared>=0.2.0        # default: no signup, no browser warning page
pyngrok>=7.0                # alternative: needs a free authtoken
nest-asyncio>=1.6
